name: github pages

on: [push]

jobs:
  deploy:

    runs-on: ubuntu-18.04

    steps:
      - name: docs
        uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          publish_dir: ./docs

